<nav class="tabs gm-primary-tabs" data-group="gm-console-primary" aria-label="{{localize 'PF2E_V2_PLAYER_CONSOLE.GMConsole.Tabs.Label'}}">
    {{#each tabs}}
        <button type="button" class="{{cssClass}}" data-action="tab" data-group="gm-console-primary" data-tab="{{id}}" aria-pressed="{{active}}">{{localize label}}</button>
    {{/each}}
</nav>
