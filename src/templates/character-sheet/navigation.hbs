<nav class="sheet-tabs" aria-label="Character sections">
    {{#each tabs}}
        <button type="button" class="{{#if active}}active{{/if}}" data-action="tab" data-tab="{{id}}" aria-pressed="{{active}}">{{label}}</button>
    {{/each}}
</nav>
